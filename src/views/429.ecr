<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<title>429 Too Many Requests — Kemal Korudu</title>
<meta name="viewport" content="width=device-width,initial-scale=1">
<style>
  :root {
    --bg: #0b0e11;
    --card: #111417;
    --text: #e6edf3;
    --muted: #9aa6b2;
    --accent: #00a3ff;
    --warning: #ffa940;
    --border: #22272e;
  }
  @media (prefers-color-scheme: light) {
    :root {
      --bg:#f6f8fa; --card:#ffffff; --text:#0a0c10; --muted:#4b5563; --accent:#0ea5e9; --warning:#d97706; --border:#e5e7eb;
    }
  }
  * { box-sizing: border-box }
  body {
    margin: 0; padding: 24px; background: var(--bg); color: var(--text);
    font: 15px/1.6 -apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Inter,Ubuntu,"Helvetica Neue",Arial;
    display: grid; min-height: 100dvh; place-items: center;
  }
  .wrap { max-width: 840px; width: 100%; }
  .card {
    background: var(--card); border: 1px solid var(--border);
    border-radius: 16px; padding: 28px; box-shadow: 0 10px 30px rgba(0,0,0,0.20);
  }
  .top {
    display:flex; gap:16px; align-items:center; margin-bottom:16px;
  }
  .logo { width:48px; height:48px; border-radius:12px; border:1px solid var(--border); }
  h1 { margin:0; font-size:22px; letter-spacing: .2px; }
  .muted { color: var(--muted) }
  .pill {
    display:inline-flex; align-items:center; gap:8px;
    padding:6px 10px; border-radius:999px; font-size:12px; border:1px solid var(--border);
    background: linear-gradient(180deg, rgba(255,255,255,0.04), rgba(0,0,0,0.05));
  }
  .pill .dot { width:8px; height:8px; border-radius:999px; background: var(--warning); box-shadow: 0 0 0 3px rgba(255,169,64,.15) }
  .status {
    margin: 14px 0 8px; font-size:48px; font-weight:700; letter-spacing:1px;
    background: linear-gradient(90deg, var(--warning), var(--accent));
    -webkit-background-clip: text; background-clip: text; color: transparent;
  }
  .msg {
    margin: 8px 0 0; font-size: 14px; color: var(--muted);
    border-left: 3px solid var(--border); padding-left: 10px;
  }
  .grid {
    margin-top:18px; display:grid; grid-template-columns: repeat(3, minmax(0,1fr));
    gap:12px;
  }
  .kv {
    border:1px solid var(--border); border-radius:12px; padding:12px; background: rgba(255,255,255,0.02);
  }
  .k { font-size:12px; color: var(--muted) }
  .v { font-weight:600; margin-top:4px; word-break: break-all }
  .help {
    margin-top:18px; padding-top:18px; border-top:1px dashed var(--border); color: var(--muted); font-size:13px;
  }
  .btn {
    display:inline-block; margin-top:14px; padding:10px 14px; border-radius:10px; text-decoration:none;
    border:1px solid var(--border); color: var(--text);
  }
  .foot { margin-top:16px; text-align:center; color:var(--muted); font-size:12px }
</style>
</head>
<body>
  <div class="wrap">
    <div class="card">
      <div class="top">
        <img class="logo" src="<%= logo %>" alt="WAF">
        <div>
          <h1>Kemal WAF</h1>
          <div class="muted">Rate limit exceeded. Please try again later.</div>
        </div>
        <div style="flex:1"></div>
        <span class="pill"><span class="dot"></span> 429 Too Many Requests</span>
      </div>

      <div class="status">Rate Limit Exceeded</div>
      <p class="msg"><%= msg %></p>

      <div class="grid">
        <div class="kv"><div class="k">Limit</div><div class="v"><%= limit %> requests</div></div>
        <div class="kv"><div class="k">Reset At</div><div class="v"><%= reset_at_str %></div></div>
        <div class="kv"><div class="k">Ray ID</div><div class="v"><%= ray %></div></div>
      </div>

      <div class="help">
        You have exceeded the rate limit for this endpoint. Please wait before making another request.
        <br>
        <a class="btn" href="/" rel="nofollow">Go back home</a>
      </div>

      <div class="foot">Served by Kemal WAF • <%= timestamp %></div>
    </div>
  </div>
</body>
</html>

